[build-system]
requires = ["networkx"]
build-backend = "flit_core.buildapi"

[project]
name = "exotic groups"
authors = [{name = "Anders", email = "accornect@mun.ca"}]
dynamic = ["version", "description"]
